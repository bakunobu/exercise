# a
def greater_smaller(a: float, b:float) -> tuple:
    if a > b:
        return(a, b)
    if b > a:
        return(b, a)
    
# b
def greater_smaller(a:float, b:float) -> tuple:
    if a > b:
        return(a, b)
    return(b, a)

