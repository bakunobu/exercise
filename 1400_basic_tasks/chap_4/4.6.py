# a

def find_value(x:float) -> float:
    if x <= 2:
        return(x)
    else:
        return(2.0)
    

# b
def find_value_two(x:float) -> float:
    if x <= 3:
        return(x)
    else:
        return(-3.0)